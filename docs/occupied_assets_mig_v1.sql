CREATE TABLE occupied_assets_mig_v1 (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    employee_code VARCHAR2(100),
    employee_name VARCHAR2(255),
    client_name VARCHAR2(255),
    mouza_name VARCHAR2(255),
    asset_name VARCHAR2(255),
    asset_type VARCHAR2(255),
    rental_type VARCHAR2(255),
    started_at DATE,
    ended_at DATE,
    khatian_no VARCHAR2(255),
    dag_no VARCHAR2(255),
    payment_term VARCHAR2(255),
    periodical_term VARCHAR2(255),
    remarks CLOB,
    unit_acr NUMBER(15, 4),
    unit_sft NUMBER(15, 4),
    rental_term VARCHAR2(255),
    unit_rate_amount NUMBER(20, 4),
    fixed_rent_amount NUMBER(20, 4),
    onetime_fee NUMBER(20, 4),
    advance_received_amount NUMBER(20, 4),
    received_amount NUMBER(20, 4),
    due_amount NUMBER(20, 4),
    fine_amount NUMBER(20, 4),
    vat_percent NUMBER(20, 4),
    received_amount_vat NUMBER(20, 4),
    tax_percent NUMBER(20, 4),
    payment_method VARCHAR2(255),
    challan_no VARCHAR2(255),
    po_or_cheque_no VARCHAR2(255),
    rate_active_from DATE,
    rate_active_to DATE,
    status CHAR(1) DEFAULT 'A',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
